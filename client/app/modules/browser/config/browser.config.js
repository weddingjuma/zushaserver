(function () {
  'use strict';
  angular
    .module('com.module.browser')
// allow this only in dev mode
  /**  .run(function ($rootScope, Event, gettextCatalog) {
      $rootScope.addMenu(gettextCatalog.getString('Browser'), 'app.browser.models', 'fa-globe');
    }); **/

})();
